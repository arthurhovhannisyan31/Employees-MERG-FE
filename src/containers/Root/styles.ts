// deps
import { makeStyles } from '@material-ui/core/styles'

export default makeStyles(() => ({
  loadingFallback: {
    position: 'absolute',
    top: 0,
    left: 0,
    width: '100%',
    height: '100vh',
  },
}))
