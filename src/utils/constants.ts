// eslint-disable-next-line
export const ROUTES = {
  HOME: '/',
  AUTH: '/auth',
  ABOUT: '/about',
  EMPLOYEES: '/employees',
  EMPLOYEE: '/employees/:id',
  NOT_FOUND: '*',
}

const CONSTANTS = {
  ROUTES,
}

export default CONSTANTS
