// eslint-disable-next-line
export const ROUTES = {
  HOME: '/',
  AUTH: '/auth',
  EVENTS: '/events',
  BOOKINGS: '/bookings',
  ABOUT: '/about',
  NOT_FOUND: '*',
}

const CONSTANTS = {
  ROUTES,
}

export default CONSTANTS
