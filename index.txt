// deps
// components
// model
// helpers

-- release 0. - ui-improvements
design improvements
remove half of loading indicators
update styles
add react helmet
add fonts
nav bar props array

-- release 0. - registration
add registration and forgot password
registration password validation
forgot email form
email reset uniq link

-- release 0. - add jest, write test

-- release 0. - advanced authorization
google login form

форма логин/регистрации на 2 таба
add CRUD for employees, title, payoff, salary, hire/fire employee
add features from material-ui project
new route, get design from material-ui examples page
analytics: table, graphs, reports
indexeddb
isAuthenticated
new Date().getTime() / 1000 < authState.expiresAt

- // local storage userInfo, expiresAt
fix fetch response types result?.data?.login?.userCredentials

- get rid of html file

-- loading spinner to loading window

-- replace all reducers with objects

-- add auth context loading state, hide login page when fetching me status

-- add service worker
-- separate vendor chunks
-- add service worker

-- run localhost on http + https

-- add a main loading spinner on reload while fetching user data

-- add explicit isAuthenticated state
-- add X-CSRF-Token
